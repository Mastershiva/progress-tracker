package com.progresstracker;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main entry point for the Progress Tracker Application
 * This is the starting point of the Spring Boot application
 */
@SpringBootApplication
public class ProgressTrackerApplication {

    public static void main(String[] args) {
        SpringApplication.run(ProgressTrackerApplication.class, args);
    }

}
